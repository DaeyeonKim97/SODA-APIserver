<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soda.apiserver.restaurant.dao.MenuMapper">
    <select id="insertMenu" resultType ="MenuDTO">
        INSERT INTO TBL_MENU
        (
              MENU_ID
            , RESTAURANT_ID
            , MENU_NAME
            , PRICE
        )
        VALUES
            (
                SEQ_MENU_ID.NEXTVAL
            , #{restaurantId}
            , #{menuName}
            , #{price}
            )
    </select>


</mapper>